import { Pill, Bone, HeartPulse, Stethoscope, Brain, Activity, Users, Wind } from 'lucide-react';

// Maps the `icon` field on each therapeuticAreas entry to its lucide-react component.
export const THERAPEUTIC_ICONS = {
  Pill,
  Bone,
  HeartPulse,
  Stethoscope,
  Brain,
  Activity,
  Users,
  Wind,
};
